{
  "rules": [
    {
      "from": {
        "host": "localhost",
        "path": "/test_from"
      },
      "to": {
        "host": "localhost",
        "path": "/test_to"
      },
      "public": false,
      "limit_config": {
        "threshold_per_sec": 10,
        "key": "ip"
      }
    }
  ]
}
